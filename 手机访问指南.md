# 📱 TOKI 手机访问指南

## 方法 1: 本地网络访问（推荐 - 立即使用）

### 步骤 1: 启动本地服务器

在电脑终端运行：
```bash
cd /home/tony/.openclaw/workspace/toki-v2
python3 -m http.server 8080
```

### 步骤 2: 查看电脑 IP 地址

**Linux/Mac**:
```bash
ifconfig | grep "inet "
# 或
ip addr show | grep "inet "
```

**Windows**:
```bash
ipconfig
```

找到类似 `192.168.x.x` 或 `10.x.x.x` 的地址

### 步骤 3: 手机访问

在手机浏览器输入：
```
http://你的电脑IP:8080/index-v4.1-mvp.html
```

例如：`http://192.168.1.100:8080/index-v4.1-mvp.html`

---

## 方法 2: GitHub Pages（推荐 - 长期使用）

### 如果已有 GitHub 仓库

```bash
cd /home/tony/.openclaw/workspace/toki-v2

# 提交更改
git add -A
git commit -m "TOKI V4.1 更新"

# 推送
git push origin main
```

### 访问地址

```
https://你的 GitHub 用户名.github.io/toki-v2/index-v4.1-mvp.html
```

例如：`https://chaseqq123.github.io/toki-v2/index-v4.1-mvp.html`

---

## 方法 3: 使用 ngrok（外网访问）

### 步骤 1: 安装 ngrok

```bash
# Linux
sudo snap install ngrok

# Mac
brew install ngrok

# 或从 https://ngrok.com 下载
```

### 步骤 2: 启动本地服务器

```bash
cd /home/tony/.openclaw/workspace/toki-v2
python3 -m http.server 8080
```

### 步骤 3: 启动 ngrok

```bash
ngrok http 8080
```

### 步骤 4: 获取外网地址

ngrok 会显示类似：
```
Forwarding: https://xxxx-xxxx.ngrok.io -> http://localhost:8080
```

在手机浏览器访问该地址即可。

---

## 🎯 推荐测试页面

| 页面 | 用途 | 推荐 |
|------|------|------|
| `index-v4.1-mvp.html` | 主界面（完整版） | ⭐⭐⭐ |
| `index-v4-mvp.html` | 主界面（简洁版） | ⭐⭐ |
| `test-aliyun.html` | API 测试 | ⭐⭐ |
| `pdf-tools.html` | PDF 工具 | ⭐ |

---

## 🐛 常见问题

### Q1: 手机无法访问？
**检查**:
1. 手机和电脑在同一 WiFi 网络
2. 防火墙是否阻止 8080 端口
3. IP 地址是否正确

### Q2: 页面加载慢？
**解决**:
1. 确保手机和电脑信号良好
2. 使用 5GHz WiFi（如果支持）
3. 关闭其他占用带宽的应用

### Q3: API 调用失败？
**检查**:
1. 控制台是否有错误
2. API 密钥是否正确
3. 网络连接是否正常

---

## 📊 手机适配特性

✅ 响应式布局  
✅ 触摸友好的按钮  
✅ 底部导航栏  
✅ 适配各种屏幕尺寸  
✅ 安全区域适配（iPhone）  
✅ 支持横竖屏  

---

## 🔗 快速链接

**主界面**: `index-v4.1-mvp.html`  
**测试页**: `test-aliyun.html`  
**PDF 工具**: `pdf-tools.html`  

---

**创建日期**: 2026-04-16  
**版本**: V4.1

# 🚀 TOKI V2 - 立即开始测试

## ✅ 已完成

- ✅ 视频通话UI界面
- ✅ WebRTC客户端代码
- ✅ 信令服务器代码
- ✅ 启动脚本

---

## 🎯 快速测试（2种方式）

### 方式1：一键启动（推荐）

```bash
cd /home/tony/.openclaw/workspace/toki-v2
./start.sh
```

**这会自动启动**：
- 信令服务器（WebSocket）
- 前端服务器（HTTP）

---

### 方式2：手动启动

**步骤1**：启动信令服务器
```bash
cd /home/tony/.openclaw/workspace/toki-v2
python3 signaling_server.py
```

**步骤2**：启动前端服务器
```bash
cd /home/tony/.openclaw/workspace/toki-v2
python3 -m http.server 8080
```

**步骤3**：打开浏览器
```
http://localhost:8080
```

---

## 📋 测试步骤

### 1. 第一个浏览器窗口

```
1. 打开 http://localhost:8080
2. 点击"创建房间"
3. 允许摄像头权限
4. 等待对方加入
```

### 2. 第二个浏览器窗口

```
1. 打开 http://localhost:8080（新窗口）
2. 输入房间ID（第一个窗口显示的）
3. 点击"加入房间"
4. 允许摄像头权限
```

### 3. 观察结果

**应该看到**：
- ✅ 左边显示你的视频
- ✅ 右边显示对方的视频
- ✅ 可以实时视频通话
- ✅ 控制按钮工作正常

---

## 🎛️ 功能测试

### 测试控制按钮

| 按钮 | 功能 | 测试 |
|------|------|------|
| 🎤 | 开关麦克风 | 点击后对方听不到声音 |
| 📹 | 开关摄像头 | 点击后对方看不到画面 |
| 🖥️ | 分享屏幕 | 点击后分享屏幕内容 |
| 📞 | 结束通话 | 点击后断开连接 |

---

## 🐛 可能的问题

### 1. 摄像头权限

**症状**：视频不显示
**解决**：浏览器会弹窗询问权限，点击"允许"

### 2. WebSocket连接失败

**症状**：状态显示"错误"
**解决**：确认信令服务器已启动

### 3. 视频延迟

**症状**：视频有延迟
**说明**：本地测试通常延迟很小

---

## 📱 下一步

### 本地测试成功后：

1. **部署到阿里云**
   - 上传信令服务器到服务器
   - 配置WebSocket端口
   - 测试公网连接

2. **配置HTTPS**
   - WebRTC需要HTTPS
   - 配置SSL证书
   - 测试HTTPS访问

3. **功能增强**
   - 多人视频
   - 屏幕共享
   - UI优化

---

## 💡 快速命令

```bash
# 一键启动
./start.sh

# 或手动启动
python3 signaling_server.py &  # 信令服务器
python3 -m http.server 8080 &   # 前端服务器

# 打开浏览器
open http://localhost:8080
```

---

**Tony，现在可以开始测试了！用 `./start.sh` 一键启动！** 🚀